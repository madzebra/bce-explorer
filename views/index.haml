= partial :about

%h2 Latest Blocks
%table.table.table-hover#recent-blocks
  %thead
    %tr
      %th Blockheight
      %th Date/Time
      %th Transactions
      %th Difficulty
  %tbody
    - @blocks.each do |blk|
      %tr
        %td= link_to_block blk['height']
        %td= time_ago blk['time']
        %td= blk['tx'].length
        %td= blk['difficulty']
